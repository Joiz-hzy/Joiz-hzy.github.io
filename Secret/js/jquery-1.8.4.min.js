$('#login-button').click(function (event) {
    var userName = document.getElementById("userName").value;
    var pwd=document.getElementById("pwd").value;
    if(userName=="lxr" &&  pwd=="19970928"){
		event.preventDefault();
		$('form').fadeOut(500);
		$('.wrapper').addClass('form-success');
		setTimeout(function(){location.href="Secret/index.html";},2000);
	}
	else{
		alert("错误！!!");
	}
});


$(document).click(function (e) {
    var list = ['生日快乐', '天天开心', '学习进步', '不亦乐乎', '健康成长', '快乐随性', '积极向上', '敢爱敢恨', '幸福长久'];
    var lists = Math.floor(Math.random() * list.length);
    var colors = '#' + Math.floor(Math.random() * 0xffffff).toString(16);
    var $i = $('<span class="animate" />').text(list[lists]);
    var xx = e.pageX || e.clientX + document.body.scroolLeft;
    var yy = e.pageY || e.clientY + document.body.scrollTop;

    $('body').append($i);
    $i.css({
        top: yy,
        left: xx,
        color: colors,
        transform: 'translate(-50%, -50%)',
        display: 'block',
        position: 'absolute',
        zIndex: 999999999999,
        fontWeight: 500
    })
    $i.animate({
        top: yy,
        opacity: .5
    }, 3000, function () {
        $i.remove();
    })
})